# Reverse proxy + sync — firewall-vm after split from OpenClaw agent stack
# Copy sync-server/ build context + .env (SYNC_TOKEN) from the mini project.
# Do NOT include OpenClaw here — OpenClaw stays on arm-mini.

services:
  sync-server:
    build: ./sync-server
    container_name: sync-server
    restart: unless-stopped
    ports:
      - "127.0.0.1:13001:3001"
    volumes:
      - sync-data:/app/data
    environment:
      - SYNC_TOKEN=${SYNC_TOKEN}
    healthcheck:
      test: ["CMD", "wget", "--spider", "-q", "http://localhost:3001/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 10s
    networks:
      - proxy

  caddy:
    image: caddy:2-alpine
    container_name: caddy
    restart: unless-stopped
    # Reverse proxy must be reachable from the LAN (domain-based routing for
    # other homelab devices), so publish on all interfaces — not 127.0.0.1.
    # Only :80 is served today; publish :443 once TLS is configured in Caddyfile.
    ports:
      - "80:80"
    healthcheck:
      test: ["CMD", "wget", "--spider", "-q", "http://localhost:80"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 10s
    volumes:
      - ./caddy/Caddyfile:/etc/caddy/Caddyfile:ro
      - caddy-data:/data
      - caddy-config:/config
    networks:
      - proxy
    depends_on:
      - sync-server
    extra_hosts:
      - "host.docker.internal:host-gateway"

volumes:
  sync-data:
  caddy-data:
  caddy-config:

networks:
  proxy:
    external: true
