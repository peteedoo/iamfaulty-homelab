# Copy to ops/stack-up.local.env and fill in real values.
# ops/stack-up.local.env is gitignored (*.env) — never commit it.

# Hostname or IP of the NAS that exports the compose/media share
NAS_HOST=<nas-host-or-ip>

# NFS export path on the NAS
NAS_SHARE=<nfs-export-path>
